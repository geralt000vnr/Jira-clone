# Progress Log

## Current status
Backend AND frontend are scaffolded, redesigned, extended with a full "Phase 1: make the board real" feature set (2026-07-14, committed as `9b0e59d`), have been through a **backend + frontend polish pass** (2026-07-16, committed as `a5c2cfa`): automated tests, pagination, admin-override deletion, optional Redis rate limiting, user-email lookup, dark mode, and a toast/confirm-dialog system replacing every `window.alert`/`window.confirm` call — and now also include **Phase 2: "planning & roadmap"**, **Phase 3: "automation rules"**, **Phase 4: "workflow transitions"**, and **Phase 5: "personal dashboard"** (all built 2026-07-16/17). Every sub-phase and every polish item was verified live — backend via 53 passing Jest/Supertest tests plus curl, frontend via the browser-preview tool (including toggling dark mode across every surface, a real automation firing off a live board drag, a real keyboard-driven drag rejected by a workflow-transition restriction, and the dashboard's gadgets updating live after an assignment/view/status-change). The scaffold + redesign + Phase 1 + polish-pass commits are pushed to the `scaffold/backend` branch on GitHub; Phase 2 + Phase 3 are committed (`6d867c1`) and pushed to `feature/roadmap-custom-fields-automation-rules` (no PR opened yet — user is handling that themselves since `gh` isn't installed here); **Phase 4 and Phase 5's changes are uncommitted, sitting on top of that same branch** (see Next up).

## Done
- Initial commit with `.gitignore`.
- Full backend + frontend architecture designed: models, middleware, controllers, routes, services, sockets, and matching React components/pages for every module (auth, orgs, projects, project members/roles, issues incl. hierarchy, sprints, comments, attachments, activity/audit trail, analytics).
- Two known gaps identified during design and already resolved on paper: `requireSprintProjectRole` middleware (sprint routes need to resolve projectId via the sprint, not the URL) and Zod validation + rate limiting on auth routes.
- **Backend fully scaffolded**: all 9 models, all middleware (incl. `requireSprintProjectRole`, `validate`, `rateLimiters`), all Zod validation schemas, all services (`FileStorageInterface`/`LocalDiskStorage`, `notificationService`, `emailService` stub, `activityLogger`, `emailQueue`), all 6 controllers, all 6 route files, Socket.io wiring, `app.js`/`server.js`. `npm install` succeeds (489 packages; one unresolved high-severity transitive audit warning from `bcrypt`'s `node-pre-gyp` → `tar` dependency chain, install-time only, not on the request path — left as-is, revisit if `npm audit fix --force` has a safe path later).
- Backend smoke-tested live: `POST /api/auth/signup`, `/login`, `POST /api/projects` (+ Zod 400 on bad input), `POST /api/issues` (transaction-backed key generation → `ENG-1`), `PATCH /api/issues/:id/move` (version 0→1, then a stale-version retry correctly returned `409`), `POST /api/sprints`, `POST /api/issues/:id/comments`, `GET /api/issues/:id/activity`.
- Set up a project-local MongoDB dev instance (`backend/scripts/dev-mongo.sh`, single-node replica set on port 27018, isolated data dir) since transactions require a replica set and the user's global mongod is standalone — chosen over reconfiguring the global mongod so other local projects aren't affected. See CLAUDE.md "Known gotchas".
- **Frontend fully scaffolded** (Vite + React 19 + Tailwind v4 via `@tailwindcss/vite`, no shadcn CLI — hand-written primitives in `components/ui/`): `axiosClient` + all 6 `api/*Api.js` modules, `AuthContext`/`SocketContext`, Kanban `Board`/`BoardColumn`/`IssueCard` with `@hello-pangea/dnd`, `FilterBar`, `IssueModal`/`IssueForm`/`CommentThread` (+ auth-gated attachment open via blob URL), `ProjectList`/`ProjectMembers`, `BacklogPanel` (sprints), `WorkloadChart` (recharts), full routing shell. Also added `CreateIssueModal` — the original design spec never included any way to create an issue from the UI, which was a real functional gap.
- Frontend smoke-tested live in the browser preview tool end-to-end (see Current status). Fixed several integration bugs surfaced only by that live testing (not visible from reading the code in isolation) — all documented in CLAUDE.md "Known gotchas": status changes must go through the move endpoint not the general update endpoint; sprint dates must be full ISO datetime strings; attachment file URLs need to be fetched with the JWT attached (blob URL) rather than used as a plain `<a href>`; the login-failure/401-redirect-loop guard in `axiosClient`.
- Added `.claude/launch.json` (`frontend` config, `npm --prefix frontend run dev`, port 5173) so the browser-preview tool can launch the dev server directly.
- **Full visual redesign pass** (2026-07-14, on request): added `lucide-react` for icons; a shared `Button` UI primitive (`components/ui/button.jsx`) and `fieldClass`/`getInitials` helpers in `lib/utils.js` so every input/select/avatar looks consistent; CSS keyframe animations in `index.css` (`fade-in`, `scale-in`, `slide-down`, `slide-up`) used for modal entrances, list-item stagger, and toast-style save confirmations. Every page/component got a pass: gradient login screen with logo mark, sticky translucent headers, colored project-card accents, priority/type icons on issue cards, animated tab underlines, avatar-initials everywhere (comments, members, assignees), skeleton loading states (board columns, project grid), and a "Saved" flash on inline issue-field edits.
- Found and fixed a real null-safety bug while verifying the redesign live: `ProjectMember.userId` can come back `null` from Mongoose `.populate()` if the referenced `User` no longer exists (e.g. a dangling reference), and `FilterBar`, `IssueCard`, `ProjectMembers`, and `WorkloadChart` all assumed it was always populated — one crashed the whole board with a blank white screen. Fixed by filtering/guarding `m.userId` at every consumption site. Not test-only: this can happen in production too (soft-deleted or removed users still referenced by a stale `ProjectMember` row).
- **Phase 1 — "make the board real"** (2026-07-14; user requested a plan against the full Atlassian Jira feature list, we scoped it down to real buildable PM features and picked this as the first phase — see the plan file this session generated). All 5 sub-phases built and verified live, in dependency order:
  - **Custom per-project workflow statuses**: new `WorkflowStatus` model (category `todo`/`in_progress`/`done` drives board grouping + "is this done?" logic; name/color/order freely editable). `Issue.status` (fixed enum) → `Issue.statusId` (ref) — the one breaking schema change in this phase, with a migration script for pre-existing databases. Board now renders dynamic columns instead of a hardcoded 3; new `WorkflowSettings` UI in Project Settings (add/rename/recolor/reorder/delete, delete blocked with `409` while in use). Verified: default seeding, custom status creation, moving a card into it, reorder, in-use delete guard.
  - **Issue linking**: new `IssueLink` model (`blocks`/`relates_to`/`duplicates`, inverse phrasing computed at read time from which side you're viewing). New "Linked issues" section in `IssueModal` with issue search-and-link. Verified both directions render correctly ("blocks" vs "is blocked by").
  - **Story points + time tracking + burndown**: `Issue` gained `storyPoints`/`originalEstimateSeconds`/`remainingEstimateSeconds`/`loggedSeconds`; new `Worklog` model for "log work" entries. New time-tracking widget in `IssueModal` (progress bar, log/delete work). `GET /api/sprints/:id/burndown` derives the actual-remaining line from the existing `IssueActivity` audit trail (no new tracking) — hit and fixed a real off-by-time-of-day bug where a same-day completion wasn't counted until the next day (day boundaries were compared against midnight instead of end-of-day). `BurndownChart` (built during the original scaffold but never wired to a page) is now live via a new `BurndownPanel` with a sprint picker.
  - **Bulk actions**: `PATCH`/`DELETE /api/issues/bulk`, scoped to the Backlog list (checkboxes + toolbar: move status, assign, delete). Each item still goes through its own optimistic-lock check — a batch can partially succeed, and the response reports which. Verified success, partial-failure (stale version), and delete via both curl and the UI.
  - **Saved filters**: new `SavedFilter` model (personal or shared-with-team), `FilterBar` gained a save-current-filter form and an apply/delete dropdown. Verified create → list → apply → actually filters the board → delete, both via curl and the live UI.
- **Backend + frontend polish pass** (2026-07-16, user requested "backend polish and frontend polish points" from the Phase-1-completion Next-up list before moving to a further feature phase):
  - **Automated backend tests**: Jest + Supertest, 8 suites / 36 tests, against a real replica-set MongoDB (`jira-clone-test` db, same `dev-mongo.sh` instance since `createIssue`'s transaction needs a replica set just like dev). Covers auth (incl. rate-limit 429 after 10 attempts, duplicate-email 409), projects (incl. a real non-admin-403 test using a `moveUserToOrg` test helper + relogin), issues (create/list+pagination/update/move/delete-cascade/bulk/links), workflow statuses (incl. delete-in-use 409 guard), sprints (incl. burndown data shape), and the new admin-override + user-lookup endpoints. **Found and fixed a real pre-existing bug via this testing**: `User.email`'s uniqueness index was scoped per-organization instead of globally, which would've let the same email sign up under multiple orgs and made login resolve non-deterministically — see CLAUDE.md gotcha.
  - **Pagination**: `GET /api/issues` takes `page`/`limit` (default 200, max 500) and returns `{ issues, total, page, limit }` instead of a bare array.
  - **Project-admin override for comment/attachment/worklog deletion**: new `services/permissions.js` (`isProjectAdminForIssue`) — a project admin can now delete other people's comments/attachments/worklogs, not just the author/uploader.
  - **Optional Redis-backed rate limiting**: `REDIS_URL` env var, `rate-limit-redis` + `ioredis`, falls back to in-memory when unset. Verified live (installed Redis temporarily via Homebrew, confirmed the `rl:::1` key and a real 429 after 10 attempts, then reverted to no-Redis for normal dev).
  - **`GET /api/users?email=` lookup** (exact-match only, to avoid enabling org-wide user enumeration) — `ProjectMembers`' "add member" form now takes an email instead of a raw Mongo user id.
  - **Dark mode**: Tailwind v4 class strategy (`@custom-variant dark`), `ThemeContext` + `ThemeToggle` (sun/moon button in the Dashboard and Project Board headers), persisted to `localStorage` with a `prefers-color-scheme` fallback on first visit. Every themed surface across the app got a `dark:` pairing; `BurndownChart`/`WorkloadChart` (recharts, inline SVG color props rather than Tailwind classes) read `useTheme()` directly. Verified live by toggling both ways on the dashboard, board, issue modal (incl. the confirm-delete dialog), and project settings — caught and fixed one real contrast bug along the way (the issue-title `<input>` had no explicit text color and was unreadable on the dark modal background).
  - **Toast + confirm-dialog system**: `ToastContext`/`useToast()` and `ConfirmContext`/`useConfirm()` (promise-based), replacing every `window.alert`/`window.confirm` call across `Board`, `IssueModal`, `WorkflowSettings`, `ProjectMembers`, and `BacklogPanel`.
- **Phase 2 — "planning & roadmap"** (2026-07-16; user chose this theme via AskUserQuestion from the Phase-1-deferred list — see the plan file this session generated). Both pieces built and verified live:
  - **Custom fields**: new `CustomField` model (`text`/`number`/`date`/`checkbox`/`select`, `select` carries `options`), values on `Issue.customFieldValues` (a plain `Mixed` object, no per-field-type server-side enforcement — same rigor tier as the rest of this schema set). New `CustomFieldSettings` UI in Project Settings (add/reorder/delete — delete has **no in-use guard**, unlike `WorkflowStatus`, since a stray key is inert). `IssueForm` renders one input per defined field, type-appropriate (text/number/date inputs, a checkbox, a `<select>` for `select` fields). Verified: created a text field ("Severity") and a select field ("Component" — API/UI/DB), set both on an issue, confirmed they persisted across reload/reopen.
  - **Roadmap**: `Issue.startDate` (paired with existing `dueDate`) gives Epics a date range; `IssueForm` only shows Start date for `type === 'epic'`. New `GET /api/projects/:projectId/roadmap` returns every Epic plus `totalChildren`/`doneChildren` derived from a live aggregate over child issues' `WorkflowStatus` category. New `RoadmapView` (new "Roadmap" tab in `ProjectBoardPage`) renders a month-scale timeline with one bar per scheduled Epic (position/width from its date range, a progress-fill overlay from done/total children), an "Unscheduled epics" list for Epics missing dates, and click-through to the existing `IssueModal` to edit dates — no drag-resize on the timeline itself (v1 scope, same tier as the existing read-mostly analytics charts). Verified: created an Epic, set start/due dates via the modal, added two child stories, moved one to Done, confirmed the Roadmap tab renders the bar at the right position with a 50% progress fill and the correct `1/2 done` tooltip.
  - **Found and fixed a real pre-existing bug while building Roadmap**: `IssueForm`'s "Due date" `<input type="date">` was sending the browser's bare `YYYY-MM-DD` value straight to the backend, which requires a full ISO datetime string (same class of bug the Sprint-dates gotcha already covered for `BacklogPanel` — this one just hadn't been hit yet). Fixed with the same `toIsoOrNull()` conversion, applied to both the existing Due date field and the new Start date field.
  - Backend test suites grew to 42 (10 suites): new `customFields.test.js` and `roadmap.test.js`, plus a `PATCH` assertion for `customFieldValues` folded into `customFields.test.js`'s delete-with-values-still-set test (a dedicated `issues.test.js` test for this was tried first but removed — it pushed that file's signup count to 11, past the 10-per-window auth rate limiter, and the coverage was redundant with the two new files anyway).
- **Phase 3 — "automation rules"** (2026-07-16; user chose this theme via AskUserQuestion from the remaining Phase-1-deferred list). Built and verified live:
  - New `AutomationRule` model (one `trigger` ∈ `issue_created`/`issue_status_changed`/`issue_assigned`/`comment_added`, one optional single-field `condition`, one `action` ∈ `set_status`/`assign_to`/`assign_to_reporter`/`add_comment`/`set_priority`) and `services/automationEngine.js` (`runAutomations`), hooked into `issueController.createIssue`/`moveIssue`/`updateIssue` and `commentController.createComment` right next to each one's existing `notifyIssueUpdate` call. Loop-safety is structural: the engine mutates `Issue`/`Comment` directly rather than calling back into those controllers, so an automation's own effects can never re-enter the trigger pipeline — no cycle detection needed. New `AutomationRuleSettings` UI in Project Settings (mirrors `CustomFieldSettings`: list with reorder/enabled-toggle/delete, an add form whose condition/action value control swaps based on the selected field/action type), mounted below Custom Fields.
  - **Found and fixed a real bug via testing**: `createAutomationRuleSchema` didn't list `enabled`, so creating a rule with `enabled: false` silently created an *enabled* one instead (Zod strips unrecognized keys rather than erroring) — caught by the disabled-rule test, not by inspection. Fixed by adding `enabled` to the create schema and threading it through the controller. See CLAUDE.md gotcha.
  - Verified live end-to-end via the browser-preview tool + the page's own authenticated `fetch` (screenshots were flaky/stuck in this session — verified via `get_page_text`, the accessibility tree, and direct network/response inspection instead, which is more reliable anyway for asserting exact state): created a "when status changes → assign to reporter" rule with no condition, moved a real board card to Done, and confirmed in the same response that `assigneeId` was already set to the reporter and the audit trail logged both the user's `status` change and the automation's `assigneeId` change; separately created a condition-gated rule ("only if moved to In Progress → set priority highest") and confirmed moving a different issue to *Done* correctly left its priority untouched.
  - Backend test suites grew to 49 (11 suites): new `automationRules.test.js` (CRUD incl. enabled toggle, trigger firing, condition match/non-match, disabled rule, `assign_to_reporter` overriding a user's own assignment in the same response).
- **Phase 4 — "workflow transitions"** (2026-07-17; user chose this theme via AskUserQuestion from the remaining Phase-1-deferred list). No new model — `WorkflowStatus` gained `allowedTransitions` (array of status ids; empty = unrestricted, the default for every existing status). `issueController.moveIssue` now checks the issue's real current status against that array before the optimistic-lock update and rejects disallowed moves with `400` (not `409` — a business-rule rejection, not a concurrency conflict). `IssueForm`'s status dropdown filters to allowed targets as a UX guard; `Board.jsx`'s drag-and-drop and `IssueModal.jsx`'s status change both got a new `400` branch alongside their existing `409` handling (toast + let the existing reconcile-via-refetch revert the optimistic UI — no new revert logic needed). `WorkflowSettings.jsx` gained a "Can move to: [ ]..." checkbox row per status. Verified live: restricted "To Do" to only allow "In Progress", confirmed the issue modal's dropdown for a To Do issue then only offered To Do/In Progress, and confirmed a real keyboard-driven board drag (`Space` to pick up, arrow keys to move, `Space` to drop — `@hello-pangea/dnd`'s built-in accessibility interaction, scripted since mouse-drag simulation is unreliable for this library) straight to Done was rejected with `400` and the card correctly snapped back to To Do. Backend test suite grew to 51 (still 11 suites — both new tests added to `workflowStatuses.test.js`, which had headroom, rather than the already-near-the-rate-limit `issues.test.js`).
- **Phase 5 — "personal dashboard"** (2026-07-17; user chose this theme via AskUserQuestion — the last of the originally-flagged Atlassian feature-gap themes besides advanced search). No new models. New `dashboardController.js`/`dashboardRoutes.js` mounted at `/api/dashboard`: `GET /my-issues` (everything assigned to the current user, org-wide, populated project + status) and `GET /activity` (last 20 `IssueActivity` rows across the org, newest first, populated actor + issue + issue's project) — both plain reads, no project-role check, consistent with how every other `GET` endpoint in this app already works. "Recently viewed" is client-side only — new `lib/recentlyViewed.js` (localStorage, dedupe + cap at 8), fed from a one-line addition to `IssueModal.jsx`'s existing `loadAll()`. New `Dashboard.jsx` component (stat tiles — assigned/in-progress/overdue counts derived client-side from `my-issues`, no extra endpoint — plus assigned-to-me, recently-viewed, and activity-feed gadgets) mounted on the existing `DashboardPage.jsx` (already the `/` landing route) above the project list. Every gadget item links out to `/projects/:projectId/board` rather than opening a cross-project issue modal — deliberate v1 scope cut, same as Roadmap's read-mostly bars.
  - Verified live: assigned an overdue issue to the logged-in user via the API (mirroring a real "someone assigns you something" flow) and confirmed the stat tiles and "assigned to you" list updated on reload; opened that issue then returned to the dashboard and confirmed it appeared under "recently viewed"; confirmed the activity feed showed the new `assigneeId`/`dueDate` changes at the top, newest first; confirmed dark mode renders correctly across all four gadgets.
  - Backend test suite grew to 53 (12 suites): new `dashboard.test.js` (assigned-only scoping for `my-issues`, newest-first ordering for `activity`).

## Next up
- Commit Phase 4 + Phase 5's backend + frontend changes — not yet committed, sitting on top of the already-pushed `feature/roadmap-custom-fields-automation-rules` branch, pending user confirmation.
- The user is opening the PR for `feature/roadmap-custom-fields-automation-rules` themselves (`gh` isn't installed on this machine — offered to install it, user declined since `gh auth login` would need their own credentials anyway). Once that PR exists, Phase 4/5 should land as either an addition to the same PR or their own follow-up PR — ask the user which they'd prefer before pushing again.
- Deferred from the polish pass: a Redis-backed **job queue** (BullMQ or similar) to replace `jobs/emailQueue.js`'s in-memory queue before multi-instance deployment — only the rate limiter got the optional Redis backing.
- The only originally-flagged theme from the 2026-07-14 Atlassian feature-gap conversation still open is **advanced/JQL-style search** — everything else on that list (custom workflows, issue linking, time tracking, bulk actions, saved filters, custom fields, roadmap, automation rules, workflow transitions, personal dashboard) is now done across Phases 1-5.
- Automation rules v1 is deliberately narrow (see CLAUDE.md): one trigger/condition/action per rule, no multi-action chains, no rule-triggered-by-another-rule cascading. If a future session wants to expand this, re-read the "Loop safety, by construction" note first — it's load-bearing, not just a scope cut.
- Workflow transitions v1 is a per-status allow-list only (see CLAUDE.md) — no visual diagram editor, no conditions/validators/post-functions on transitions themselves. That's a distinct, bigger feature if ever wanted.
- Personal dashboard v1 doesn't open issues in-place (every item link-outs to the project board instead) — see CLAUDE.md. The raw activity feed also shows unresolved `assigneeId`/`dueDate` values (ids/ISO strings, not names/formatted dates) — fine for a v1 feed, but worth polishing if this gadget gets more use.

## Decisions & notes
- Stack and every architectural decision below came out of a single planning conversation on 2026-07-14; the full file-by-file spec (every model/controller/route/component, with code) lives in that conversation's history, not in this repo's committed files (though the actual working code from it is now in `backend/` and `frontend/`). If a future session needs the original planning rationale rather than what's in CLAUDE.md/the code itself, ask the user to re-share that conversation.
- **MongoDB must be a replica set** for the backend to fully work (transactions in issue creation) — see CLAUDE.md "Known gotchas" for the `dev-mongo.sh` setup. Don't assume a plain local `mongod` is sufficient.
- **GitHub push access was resolved** (2026-07-14): this machine's git credentials are for GitHub user `NeerajSkeyit`; the user added that account as a collaborator on `geralt000vnr/Jira-clone` and accepted the invite, and a subsequent push succeeded. If push fails again, check the invite/collaborator status first before assuming it's a new problem.
- **Multi-tenant**: `organizationId` on every collection, auto-scoped via `req.scope()`. Never query a tenant-owned collection without it.
- **Per-project roles**: `admin`/`manager`/`developer`/`tester`, stored on `ProjectMember`, differ per project for the same user. Sprint-id-keyed routes need a project lookup via the sprint first (`requireSprintProjectRole`), not the plain `requireProjectRole`.
- **Issue hierarchy**: Epic → Story/Task/Bug → Subtask via `parentId`; subtasks can't nest further.
- **Customizable workflows**: `Issue.statusId` refs a per-project `WorkflowStatus` (name/color/order editable; category `todo`/`in_progress`/`done` fixed and drives board/sprint logic). See CLAUDE.md for the full breaking-change/migration note.
- **File storage**: local disk now, behind `FileStorageInterface` so S3 (or similar) is a drop-in swap later, not a rewrite.
- **Concurrency**: optimistic locking via `Issue.version` + `expectedVersion` on every mutating call, including bulk ops (per-item, not all-or-nothing); `409` (or a per-item `failed` entry for bulk) on mismatch, client refetches.
- **Audit trail**: `IssueActivity` per field change, skips no-op changes. Sprint burndown's "actual" line is derived entirely from this trail — no separate burndown-tracking mechanism.
- **Soft deletes** everywhere via `deletedAt`; `req.scope()` filters it out automatically. Deleting an issue cascades soft-delete to its subtasks (single or bulk delete).
- **Real-time**: Socket.io room per project board; broadcasts + async email queue on issue/comment/attachment/worklog changes.
- **Custom fields**: `Issue.customFieldValues` is a `Mixed` object (not a Mongoose `Map`), keyed by `CustomField._id`; no server-side enforcement of a field's own `type`/`options` on the value. Deleting a `CustomField` never blocks even if issues have values for it.
- **Roadmap**: Epic-only date range (`startDate`/`dueDate`), progress computed live via aggregate (no denormalized progress field to keep in sync). Read-mostly UI — editing happens through the existing `IssueModal`, not on the timeline itself.
- **Automation rules**: single trigger/condition/action per rule (no chains). Loop-prevention is structural, not a runtime guard — the engine writes directly to `Issue`/`Comment` models instead of calling the controllers that fire triggers, so automation effects can never recurse. Automation-driven `Issue` writes skip the `version` optimistic-lock check (no client is waiting on a specific version for a system-triggered write).
- **Workflow transitions**: per-status allow-list (`allowedTransitions`), empty = unrestricted by default. Enforcement lives in `moveIssue` only — `updateIssue` never touches `statusId` at all (unchanged from before this phase), so there's exactly one place transitions can be violated and exactly one place they're checked. `400` on rejection, distinct from the existing `409` optimistic-lock conflict.
- **Personal dashboard**: two new `GET`-only endpoints (`/api/dashboard/my-issues`, `/api/dashboard/activity`), no new models, no project-role gating (reads are org-wide by existing convention). "Recently viewed" is deliberately client-only (localStorage) — it's a per-device concept, not something worth a backend round-trip or a new collection for.
