PORT=5000
MONGO_URI=mongodb://localhost:27017/jira-clone
JWT_SECRET=change-this-to-a-long-random-string
CLIENT_URL=http://localhost:5173
EMAIL_PROVIDER_API_KEY=

# Optional. When set, auth rate-limit counters are stored in Redis (shared across
# instances) instead of in-memory. Leave unset for local dev — falls back cleanly.
REDIS_URL=
